#include <iostream>
using namespace std;
int main()
{
    int n;
    cin >> n;
    int sum1 = 0;
    int sum = (n) * (n + 1) / 2;
    int i = 1;
    while (i <= n)
    {
        int n1;
        cin >> n1;
        sum1 = sum1 + n1;
        ++i;
    }
    cout << (sum-sum1);
}
